// self_report.jsx
import React from 'react';

const SelfReport = () => {
  return (
    <div>
      Self Report component has rendered.
    </div>
  );
};

export default SelfReport;
// first one: get their email. f00. no name yet. just needs text entry box and a nextScreen button.

// after the test:
